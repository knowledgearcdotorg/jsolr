<?xml version="1.0" encoding="UTF-8"?>
<project name="JSolrSearch Example Plugin" default="" basedir=".">
    <property name="dest.plugins.search" value="${dest.plugins}/example"/>
    <property name="dest.plugins.search.example" value="${dest.plugins}/jsolrsearch/example"/>
    
    <target name="update" description="Copies JSolr Search Example plugin to existing Joomla install.">
        <echo message="Deploying JSolr Search Example plugin to Joomla! environment."/>
        <copy todir="${dest.plugins.search.example}">
            <fileset dir="${src}" defaultexcludes="true">
                <include name="**"/>
                <exclude name="**/language/**"/>
            </fileset>
        </copy>
        <copy todir="${dest.administrator.languages}">
            <fileset dir="${src}/language" defaultexcludes="true">
                <include name="**/**"/>
            </fileset>
        </copy>
    </target>
    
    <target name="package" description="Creates a JSolr Search Example plugin package.">
        <echo message="Packaging JSolr Search Example plugin."/>
        <zip destfile="${package.dest}/plg_jsolrsearch_example.zip"
               basedir="${src}"
               update="true"
               excludes="build*.xml"/>
    </target>
</project>